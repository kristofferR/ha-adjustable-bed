{
  "name": "adjustable-bed-card",
  "version": "0.1.0",
  "private": true,
  "description": "Native Lovelace card for the Home Assistant Adjustable Bed integration",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "bun run build.mjs",
    "watch": "bun run build.mjs --watch",
    "typecheck": "tsgo --noEmit",
    "check": "bun run typecheck && bun run build"
  },
  "dependencies": {
    "lit": "^3.2.1"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@typescript/native-preview": "^7.0.0-dev.20250601.1",
    "esbuild": "^0.24.2",
    "typescript": "^5.7.3"
  }
}
